
{{whiteBright "Usage:"}} jerry branch TICKET_ID

Create a branch in your current git repo that matches the Jira issue summary for the given ticket ID. If any branch currently matches the ticket number, it will be checked out instead of created.

{{whiteBright "Options:"}}

    --preview, -p     Show the generated branch name
    --copy, -c        Copy the generated branch name
