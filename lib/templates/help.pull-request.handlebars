
{{whiteBright "Usage:"}} jerry pull-request

Create a pull-request from the current branch using the branch's ticket ID for the info.

{{whiteBright "Options:"}}

    --into=BRANCH       Specify the branch to base the pull request on.
                        (default: config.defaultBranch)
